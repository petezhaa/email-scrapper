Put your resume here as a PDF and point config.yaml -> paths.resume at it.

By default config.yaml expects:  resume/your_resume.pdf

This file is attached to every email that gets sent.
(PDFs in this folder are gitignored so your resume isn't committed.)
