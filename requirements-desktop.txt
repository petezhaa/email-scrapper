# Extra dependencies needed only to BUILD the standalone desktop app.
# Install with:  pip install -r requirements.txt -r requirements-desktop.txt
pywebview>=5.0
pyinstaller>=6.0

# macOS needs the Cocoa/WebKit bindings for the native window (no-ops elsewhere).
pyobjc-core>=10.0; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=10.0; sys_platform == 'darwin'
pyobjc-framework-WebKit>=10.0; sys_platform == 'darwin'
